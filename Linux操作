清理Linux内存缓存 sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches'
查看linnux内存使用情况 free -m
解压tar -zxvf java.tar.gz  -C /usr/java

添加路由 route add -net 9.123.0.0 netmask 255.255.0.0 gw 9.123.0.1
用户权限 sudo chown -R hadoop ./hbase
生成随机数文件 sh iteblog.sh > data1


hadoop打包操作 
向usr/local/hadoop/etc/hadoop/hadoop-env.sh中加入
export HADOOP_CONF_DIR=/usr/local/hadoop/etc/hadoop   --启动出错（改）
export HADOOP_CLASSPATH=/usr/local/hbase/lib/* --把hbase的lib加入
1、点击Maven中的packaging完成打包，jar包的路径为linux下的路径，后面填写主类
hadoop jar Hadoop_learn-1.0-SNAPSHOT.jar Hadoop.Hbase.HbaseDamo.ExampleForHbase


mysql 
     service mysql start #启动mysql服务
     mysql -u root -p  #登陆shell界面



